# =============================================================================
# Configuracion del Backend BFF - Blinds Control
# =============================================================================
# Copia este archivo a .env y configura los valores reales:
#   cp .env.example .env
#
# IMPORTANTE: Nunca commitear el archivo .env con valores reales.
# =============================================================================

# Puerto del servidor backend
PORT=3000

# Host del API Z-Way (IP o hostname de tu controlador Z-Way)
ZWAY_HOST=192.168.1.109

# Puerto del API Z-Way
ZWAY_PORT=8083

# Protocolo para conectar con Z-Way (http o https)
ZWAY_PROTOCOL=http

# Token de sesion Z-Way (zwaysession)
# IMPORTANTE: Este valor debe ser inyectado desde un Kubernetes Secret
# o variable de entorno segura. NUNCA commitear el valor real.
ZWAY_TOKEN=

# Origenes CORS permitidos (separar con comas si son varios)
# Usar * para permitir todos (solo desarrollo)
CORS_ORIGIN=*
