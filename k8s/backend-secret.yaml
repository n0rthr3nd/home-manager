# =============================================================================
# Kubernetes Secret para el token Z-Way
# =============================================================================
# IMPORTANTE: Este archivo es una PLANTILLA. NO commitear con valores reales.
#
# Para crear el secret en el cluster:
#   kubectl create secret generic blinds-backend-secret \
#     --from-literal=ZWAY_TOKEN=tu-token-real-aqui \
#     -n default
#
# O aplicar este archivo despues de codificar el token en base64:
#   echo -n "tu-token-real" | base64
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: blinds-backend-secret
  namespace: default
  labels:
    app: blinds-backend
type: Opaque
data:
  # Reemplazar con el token codificado en base64
  # echo -n "TU_TOKEN_REAL" | base64
  ZWAY_TOKEN: ""
